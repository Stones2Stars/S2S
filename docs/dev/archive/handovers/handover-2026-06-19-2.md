# Handover 2026-06-19 (#2) — logging consolidation → §0 decisions → modifier pilot → calculator + memory restructure

> A handover is a TRANSIENT relay (work done + work next), NOT a source of truth. Every durable fact/ruling below
> already lives in a committed doc (cited). This file is deletable-without-loss. **Read the durable docs first.**
> (Sibling: `handover-2026-06-19.md` was the align/organize session that PRECEDED this one.)

## What this session did (11 commits on `json-data-migration`, all local — owner builds from the working tree)

`6618a10e6` retire C2C.log + war-ally→`[DIP/warally]` + `SFT_PLAYER name(id)` · `c1488a22a` name-change DOMAIN event ·
`bcd61389e` `addStr`/`SFT_STR` + `[UNT/act]` off legacy · `a1d0ffe41` R-5 A-1 (OutputDebugString echo removed from all
BBAI helpers + inventory correction) · `c2547a636` §0 decisions + Surveillance-knob deferral · `9536c0552` **modifier
pilot increment 1 (parse)** · `b8545b9e5` increment-1 verified + endpoint ruling · `3f1abc344` **increment 2 (deposit-flow
+ effective + `/diagnostic/modifier`)** · `23245bcd2` **pluggable calc-flow + legacy-flat-outside** · `a46ca0016` **modcalc
seed** + emulator scope · `384d2edbf` re-home memory tidbits to repo.

**Three arcs:**
1. **Logging consolidation** (the early thread): C2C.log retired; genuinely-valuable lines MIGRATED not deleted (war-ally
   → `[DIP/warally]`); `SFT_PLAYER` renders `name(id)`; the **name-change DOMAIN event** (Orwell-bar requirement + the
   civic/empire-name bug lever); `addStr`/`SFT_STR` string-pointer field (rule: the CONSUMER composes the line, the call
   site hands ingredients) with `[UNT/act]` as first user; R-5 A-1 done + the inventory's "fires in FinalRelease" error
   corrected (`OutputDebugString` is `#define`d to nothing under `FINAL_RELEASE`).
2. **§0 cascade decisions settled** (unblocked the modifier build): **tally = PLAYER-LEAF** (accepted, not city-leaf);
   **modifier-shadow names LOCKED** (R-M5); **Surveillance knob DEFERRED** to near-live.
3. **Modifier pilot — the #430 build thrust** (increments 1+2 + calc-flow), plus the **external calculator** seed.

## ⭐ RESUME HERE — the modifier external calculator / emulator (owner's priority)

The owner expanded the modcalc seed into a major workstream: a **complete external emulation of ALL legacy value
calculations**, real-data-fed, dual-implemented (legacy + cascade). It is **load-bearing for the DESTROY pass**
(map-before-delete) and is the project's de-facto integration/unit-test harness. Decisions locked (owner 2026-06-19):
- **Loadout = LIVE game-dump** (pull a real city's input vector from a new endpoint, validate against the same city's live outputs).
- **Sequencing = channel-by-channel in demolition order** (city-yields first, then commerce-split/health/happiness/defense/maintenance/unit-stats).

**Concrete NEXT build (not started):** a **`GET /diagnostic/cityInput?player=N&city=M`** endpoint in `CvHttpServer.cpp`
dumping a real city's full input vector (base components, present buildings/techs/civics/bonuses, specialists, population)
PLUS the live legacy + cascade outputs. Then extend `Tools/ModifierCalc/` to consume it, reproduce both formulas offline,
and **validate its legacy side against the live `getYieldRate100`** (that match = the proof the emulator faithfully maps
the legacy calc → the DESTROY-pass credential). Graduate it to its own plan doc as step one. Full plan + scope:
`modifier-cascade-shadow-spec.md` §3.1a.

*(Alternative/parallel: modifier pilot increment 3 — the in-game all-cities `/diagnostic/modifierSweep` + `[MODSHADOW]`
per-turn line — but the emulator is the stated priority.)*

## Key findings this session (durable homes cited — this handover holds NO facts)

- **Legacy value arithmetic was NEVER deliberately designed** ("modifiers flow top down" was the only principle); the
  `/diagnostic/modifier` endpoint surfaced it. **The cascade is the first deliberate design of the formula.** → calc-flow
  is a single SWAPPABLE dispatch point (`ModifierCalcFlow` enum + `cascadeModifierApply`); current = `CALCFLOW_LEGACY_FLAT_OUTSIDE`
  (legacy adds building flat OUTSIDE the percent; folding it inside would force a data rebalance). GOAL = parity-ADJACENT,
  not exact. Home: `modifier-cascade-shadow-spec.md` §3.1a.
- **Modifier scope model = the full containment spine** (`world/team/empire/area/city/plot/self/specialist/unit`); PLOT is
  first-class + ENCAPSULATED (reports a rolled-up total to the city; city never sees per-plot detail); vicinity checks are
  REQUIREMENTS (enabler predicates), not modifier; TEAM is a real modifier scope. Home: §3.1a + `CvCascadeModifier.h`.
- **Tally player-leaf accepted** (`tally-cascade-spec.md` + `cascade-engine-430.md` §Impl Status). **Modifier names locked**
  (`modifier-cascade-shadow-spec.md` §6/§0a). **Naming/string rulings** (`name(id)` additive; consumer-composes; name-change
  event REQUIRED for the Orwell bar): `event-spine-spec.md` §3/§8.

## Session-end state (READ before touching code)

- **All 11 commits local on `json-data-migration`, Assert-clean.** Owner builds from the working tree; push when ready.
- **The game is UP** on the increment-2 DLL — owner verified `/diagnostic/modifier?player=0` returns the cascade-vs-legacy
  decomposition on real cities (the divergence is expected: pilot deposits city-scope buildings only + the calc-flow finding).
- **Local memory was RESTRUCTURED (owner ruling 2026-06-19):** `MEMORY.md` is now sensitive-ONLY + a loud "READ ALL THE
  DOCS" pointer (25.5KB→2KB; the bloat had truncated the sensitive entry out of context). 76 repo-duplicated memories
  deleted, their genuine tidbits re-homed to the repo (`384d2edbf`). Memory = sensitive-only; repo is authoritative.
