# Handover 2026-06-19 — align/organize/find-holes session + the FIX-NOW list

> A handover is a TRANSIENT relay (work done + work next), NOT a source of truth. Every durable fact/ruling below
> already lives in a committed doc (cited). This file is deletable-without-loss. **Read the durable docs first.**

## Session mission (owner 2026-06-19)
*"Dedicated to aligning, organizing, and finding holes in both docs and current implementation alike. Small hole →
plug now. Big hole → note in the right doc. End of session → a 'fix now' list + handover that I do next session."*

## What was done
- **Specced the modifier-cascade shadow/test plan** → `docs/dev/plans/modifier-cascade-shadow-spec.md` (parity-first
  scaffold, city-yields pilot, `/diagnostic/modifierSweep` + `[MODSHADOW]`, the **Fine→Meltdown CARE SCALE**). Owner
  rulings R-M1..R-M4 captured there. The modifier engine is UNBUILT beyond the `CvModifierSlot` combine primitive.
- **Adversarial audit of the uncommitted session work** (5 minions): NO blockers; shadow discipline intact; snapshot
  isolation preserved. Committed as **`ef5c7cbba`** (event-spine logging consolidation + §14 H maintainer shadows + live
  cameras + the audit fixes + the two new plan docs).
- **Small holes plugged:** `[STATE/game|fin|dip|city]` tag drift in 3 docs + 2 stale code comments; WAI `[build/cand]`
  `culture` shadow field; `observability/README` rung (5 Meta); a resolved "tooling gap"; doc index + cross-links.
- **Big holes noted in docs:** `sources-structural-cleanup.md` (the structural campaign); the verified **§Implementation
  Status table in `cascade-engine-430.md`** (substrate built · tally PARTIAL/player-leaf · enabler GATE-built/GENERATOR-inert
  · modifier ABSENT · grants ABSENT · readJson gate-surface-only); honesty flags on `event-spine-spec` §8 + `tally-spec`.

## Durable homes (this handover holds NO facts)
- `cascade-engine-430.md` **§Implementation Status** — the ground-truth build maturity (the alignment record).
- `modifier-cascade-shadow-spec.md` — modifier build+shadow plan + care scale + R-M1..R-M4.
- `sources-structural-cleanup.md` — the structural campaign + the load-bearing ordering.
- `tally-cascade-spec.md` (⚠ divergence note) · `event-spine-spec.md` §8 (status flags) · `cascade-mapping-inventory.md`
  (the §14 H shadows) · `logging-surface-inventory.md` (the logging consolidation + anomalies).
- `boost-situation.md` — the dual-Boost (1.32 + renamed-1.55) analysis + the EXE/toolchain lock (awareness; ticket #442).

---

## ⭐ RESUME HERE — back to the MOD
The align/organize/find-holes detour is done. Next session resumes **#430 cascade**: settle the **FIX-NOW §0 owner
decisions**, then the **§2 build roadmap** — the **modifier shadow** (`modifier-cascade-shadow-spec.md`) is the natural
next build once its §6 names are locked. Everything is committed and builds green.

**Session-end state (READ before touching code):**
- **`Sources/` was REORGANIZED** into flat broad buckets (`Engine/AI/Infos/Cascade/Repos/Infrastructure/Tools/UI/Python/
  Defines` + PCH glue at root). Cross-module includes are now **path-qualified** (`"Bucket/Header.h"`); same-folder bare.
  The IDE project is **`S2S.vcxproj`/`.sln`** (was `C2C (VS2019)`). **fbuild recursively globs** — a new `Sources/<dir>/`
  needs NO `.UnityInputPath` edit (AGENTS.md "Adding a new source subdirectory" updated).
- **11 commits this session on `json-data-migration`, all UNPUSHED (`ahead 11`)**, history clean. Owner builds from the
  working tree; push when ready. Assert build GREEN (verified at `UnityNumFiles` 40 AND 256).

---

## ⭐ FIX-NOW LIST (next session — ordered)

### 0. OWNER DECISIONS that gate the work (resolve first)
- **Tally model:** accept the shipped **player-leaf** tally (rewrite `tally-cascade-spec` to match) OR build the
  spec's **city-leaf** substrate? (Code silently diverged — `cascade-engine-430` §Impl Status / `tally-spec` ⚠ note.)
- **Modifier shadow §6 names** to lock before building: `/diagnostic/modifierSweep`, `[MODSHADOW]`,
  `cascadeModifierParityMode`, `modifier-cascade-known-discrepancies.md`; parity toggle const-vs-BUG-option; rung-1 tolerance.
- **The ~31 unrecoverable logging lines** (event-spine-spec §8): carry entity IDs + resolve at display, or enum-ify?

### 1. STRUCTURAL CLEANUP CAMPAIGN — `sources-structural-cleanup.md` (owner's ask; ORDER IS LOAD-BEARING)
Decisions LOCKED this session: **flatten** (no `Base/` — subsystems top-level), broad buckets
(`Engine/AI/Infos/Cascade/Repos/Infrastructure/Tools/UI/Python/Defines` + root glue), rename →
**`S2S.sln`/`S2S.vcxproj`**, **path-qualified cross-module includes** (root-relative) + same-folder bare +
recursive-glob fbuild + PCH glue stays at root.

**✅ STRUCTURAL CLEANUP DONE THIS SESSION (Assert-clean + perturbation-verified). Commits:**
- `84660521d` recursive-glob fbuild (Phase 1).
- `759a4a73d` collision-proof: unique `<DOMAIN>F_` field prefixes, 1191 repl/12 files (the collision map turned
  out wider — CF_ 3-way Cit/Com/Ctb, DF_/EF_/WF_ pairs; multi-file mirrors safe via case/_id; full map §1A).
- `5a776a004` the MOVE: 354 files → flat buckets, 1866 cross-bucket includes path-qualified, PCH glue at root,
  no `/I` change. Fixed the exposed latent issues (case-mismatch `IDValuemap.h`; PlotSnapshot transitive Info deps).
- `faade99da` regenerated `S2S.vcxproj`/`.filters` from disk + renamed `C2C (VS2019)`→`S2S` everywhere
  (incl. functional `_UpdateVSUserFile.ps1`); updated all stale filename refs in docs/scripts.
- **Verified:** Assert build green at `UnityNumFiles=40` AND at `256` (≈2 files/blob) — robust to batch reshuffle,
  so "no random missing includes" holds. Tools: `migrate_structure.py`, `fix_log_tag_prefixes.py`, `fix_includes.py`,
  `regen_project.py`, `rename_proj_refs.py`.

**⏭ REMAINING (structural):**
- **`CvInfos.h` umbrella retirement — ZERO-RISK SLICE DONE (`b92da1a6a`: dropped the umbrella from the 3 genuine
  non-users), the rest ATTEMPTED 2026-06-19 then REVERTED to green (fragile tail).** Got 853→16 errors but
  a blind script hit include CYCLES injecting into foundational/EXE-bound headers (CvInfoBase.h/CvEnums.h/CvCity.h/...)
  + the art-info chain. It's a **dedicated careful pass, not a scripted run** — full hard-won lessons + the recommended
  redo order are in **`sources-structural-cleanup.md` §1B EXECUTION NOTES** (detect-by-accessor; never inject into
  foundational headers; headers use forward-decls; CvArtFileMgr keeps the umbrella; ~178 files, 118 are pure dead-include
  removal). Reusable scripts kept WITH a WIP caveat: `Tools/retire_cvinfos_umbrella.py`, `Tools/fix_info_includes.py`.
- **Boost 1.32 vs 1.55** — ANALYZED → `docs/dev/reference/boost-situation.md` + **ticket #442** (consolidate to a
  single Boost; sever the Boost.Python 1.32 pin). Cleanup not modernization (the EXE lock stays). Still deferred.
- **PlotSnapshot** (owner 2026-06-19): minor — diagnostic tooling, belongs nearer the logging tooling / light rework; non-critical.

### 2. #430 BUILD ROADMAP (the big unbuilt pieces — `cascade-engine-430.md` §Implementation Status)
- **Modifier machine** — build per `modifier-cascade-shadow-spec.md` (after the §0 names are locked).
- **`grants` consumer** — entirely absent; needs a build (and a shadow akin to the others).
- **Tally domain coverage** — only buildings+units wired; tech/civic/religion/bonus/project/specialist atoms silently read 0.
- **Enabler `enables`-family GENERATOR + `disables` parsing** — the generation half is inert (`cascadeTechReachable` dead).

### 3. SMALL DOC PLUGS (quick, didn't block this session)
- `data-model-spec` §2.1: add `ATOMDOMAIN_HERITAGE` (implemented-but-undocumented).
- `workedBy` / `natureYield` predicates: specced-but-unparsed → implement or drop-with-reason at parse.
- `notConstructible`: parsed into `CvEntityAvailability` but never consumed in any building gate (inert).

### 4. MEDIUM
- Dormancy shadow: cross-turn parse cache (uncached per-turn JSON IO when `gPlayerLogLevel>=1`).
- CTB mixed-gate `/events` blind spot (`logging-surface-inventory` A-16): align gate + shadow the `%S` line.

### 5. LOGGING CONSOLIDATION — finish it (`event-spine-spec` §8 / `logging-surface-inventory`)
- Per domain: verify legacy-vs-spine parity on `/events`, then CUT the legacy `log<Domain>AI` calls + retire the helper.
- Rework the Autolog BUG option → one 0–5 "Surveillance level" knob (use the `add-bug-option` skill).
- Flagged dragons: `[HAI]`/`HunterAI` rename; the Python logging structure (deferred).
