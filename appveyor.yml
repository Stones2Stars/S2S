version: 1.BETA.{build}
# skip_tags is important or you will get an infinite build loop when we apply release tags
# testing itzabird appveyor
skip_tags: true
skip_branch_with_pr: true

# TEMPORARY (2026-06-10, owner request): CI builds only on the release branch.
# We are building locally during day-to-day development, so neither main pushes nor
# PR builds run in AppVeyor (PR builds trigger off the BASE branch matching this
# filter; with only "release" here, PRs targeting main are skipped). To re-enable
# main/PR builds, add "- main" back to this list.
branches:
  only:
    - release

environment:
  svn_url: https://svn.code.sf.net/p/stones2stars/code/trunk
  release_prefix: Stones2Stars
  build_dir: _build
  release_branch: release
  svn_user: s2sappveyour
  svn_pass:
    secure: T8MivnrThpqDptrSafu2kWSvadsSCOtu9KkamwqNm20=
  # SourceForge's gateway 504s on a single whole-release commit, so the deploy
  # pushes the payload in bounded batches (Tools/CI/SvnBatchCommit.ps1).
  # A batch closes on whichever cap it hits first; lower these if 504s return.
  svn_batch_files: 400
  svn_batch_megabytes: 64
  svn_batch_retries: 5
  WEBHOOK_URL:
    secure: 6hg1/q82Oatu+Swl6MRrqf81pFqjPTwPSfIDwzoERyCfYbjWeE+xm6NObTx+8MwGGxDmvpYKs0olhCbgp5txi4eBO/Er5o7eHydLganoCvIld/bPMP8LrzIMKoEZOGfryI5D0/qSBFdgmyzVrmaX7PVkRzYZJQUSQLUucJXUHls=
  git_email:
    secure: trN7d/FWgBATcyR/BWbF313Avez3hsRfOq4PdxNcll0=
  git_user: flabbert
  git_access_token:
    secure: l8Yo3AXBjR0Z/hxaaQlvcHwdbcnUKMbpOYI0YQWon/PJcOUkC4YCZoSB6kQY1tSs

build_script:
  - cmd: >-
      call Powershell.exe -NoProfile -ExecutionPolicy Bypass -File "%APPVEYOR_BUILD_FOLDER%\Tools\CI\fix_sources_timestamps.ps1"

      call "%APPVEYOR_BUILD_FOLDER%\Tools\_AppveyorTestBuild.bat"

test_script:
  - cmd: >-
      cd Tools

      XmlValidator.exe -a

deploy_script:
  - cmd: >-
      call "%APPVEYOR_BUILD_FOLDER%\Tools\CI\DeployBuild.bat"

      call "%APPVEYOR_BUILD_FOLDER%\Tools\_TrimFBuildCache.bat"

on_success:
  - ps: Invoke-RestMethod https://raw.githubusercontent.com/DiscordHooks/appveyor-discord-webhook/master/send.ps1 -o send.ps1
  - ps: ./send.ps1 success $env:WEBHOOK_URL
on_failure:
  - ps: Invoke-RestMethod https://raw.githubusercontent.com/DiscordHooks/appveyor-discord-webhook/master/send.ps1 -o send.ps1
  - ps: ./send.ps1 failure $env:WEBHOOK_URL