# Handover 2026-06-18 — buildings cascade reconciled; NEXT = build the §14 H maintainer shadows

> A handover is a transient task relay (work done + work next), NOT a source of truth. Every durable fact/ruling below
> already lives in the committed docs cited; this file is deletable-without-loss. **Read the durable docs first** (the
> active-read-and-verify-docs ruling, AGENTS.md Conventions) — do not work from this summary alone.

## Where we are
The #428/#430 **buildings cascade is reconciled**: `/diagnostic/sweep?type=buildings&player=0` is at **16 divergences,
all UI-layer-acceptable** (`alreadyQueued` ×12 + `replaced`-via-`HIDE_REPLACED` ×4) — **zero genuine cascade gaps, zero
under-offers**. Verified live this session (owner deploys the working tree; agent reads the sweep). Branch `json-data-migration`,
working tree clean.

This session closed (all committed): notConstructible, two religion gates, building-group inheritance (cap+tech+obsolete),
bWater→IS_COASTAL, operate-OR-civics, the `replaces` subtraction, latitude (`PRED_LATITUDE`+min/max), **terrain/feature/
improvement VICINITY** (one `ccVicinityMatch` over the current workable radius), **map-category** (`PRED_HAS_MAP_CATEGORY`,
CENTER-plot, for space scenarios), **area-size** (`ATOMDOMAIN_AREASIZE` land atom + water→IS_COASTAL). Plus the legacy/cascade
block-reason diagnostics, the post-compaction-reload hook fix, and the active-read-docs ruling.

## NEXT TASK (start here in the new context)
**Build the §14 H state-maintainer behaviour shadows — the RUNTIME twin of the buildability sweep.** Rationale (owner
rulings, enabler-spec §14): zero buildability-divergence is NOT "safe to delete" — the sweep excludes `!hasBuilding`, so it
never exercises the per-turn state maintainers. Each maintainer must be shadow-verified (does the cascade replicate it, turn
over turn?) before deletion, and the bias is **over-reach** (fold a quirk in when unsure).

Start with the **riskiest cluster — AUTO-PLACEMENT** (they mutate the building set per turn via `changeHasBuilding`):
the autobuild loop (`CvCity.cpp:1459-1487`) + `checkPropertyBuildings` (`CvCity.cpp:1490-1518`, the `BUILDING_EFFECT_*`
property-band add/remove). Design a diagnostic (parallel to `/diagnostic/sweep`) that, per city per turn, compares the
maintainer's placed/removed set against what the cascade's `autoBuild`+`requires` would place/remove.

## Read FIRST (durable, load-bearing — the handover holds none of this)
- **`cascade-mapping-inventory.md` §B** — the §14 H maintainers deep-dived 2026-06-18: each maintainer's current `file:line`,
  cascade replacement, and the shadow it needs (B-i auto-placement, B-ii dormancy, B-iii group, B-iv waiver). THIS is the next-task spec.
- **`enabler-cascade-spec.md` §14** (REFRESHED 2026-06-18) — the full demolition list (A–F gates/call-sites/caches refreshed to
  current `file:line`; H maintainers) + the over-reach + map-before-delete rulings. §3 (PropertyEffect reverse-enabler), §8 (vicinity).
- **`data-model-spec.md`** — §2.5 predicates (incl. the new `HAS_IMPROVEMENT`/`HAS_MAP_CATEGORY` + the improvement-GROUPS/ranks
  TODO), §4.2b (`autoBuild`/`notConstructible`/`grants`).
- **`cascade-known-discrepancies.md`** — the 16 remaining (all UI-acceptable) + every fixed cluster, cause-tagged.
- **`docs/dev/reference/http-server.md`** — the `/diagnostic` surface (sweep + the legacy/cascade block-reason reporters) to extend.
- `AGENTS.md` Conventions — the active-read-docs ruling (the discipline that drove this session's corrections).

## Diagnostics to reuse
`GET /diagnostic/sweep?type=buildings&player=0` (buildability) + `GET /diagnostic/canConstruct?type=BUILDING_X&player=0`
(`legacyReason`/`cascadeReason`). Server gated by the `Autolog__HttpServer` BUG option; owner runs the game, agent reads. The
maintainer shadow is a NEW surface alongside these.

## Open follow-ups (lower priority, documented in place)
- Improvement GROUPS (`{HAS_IMPROVEMENT_GROUP:G}`) modelled on `PromotionLineInfo`/`Category`, with ranks → rank-derived buff +
  obsoletion (data-model §2.5 TODO).
- The 16 UI-acceptable divergences are a UI-layer concern (the python build-list applies `HIDE_REPLACED`/queue filters), not cascade.
