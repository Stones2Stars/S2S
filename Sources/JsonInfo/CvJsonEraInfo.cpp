//
//	CvJsonEraInfo -- see the header. The composed units carry the era's JSON data; no typed members yet, so there
//	is no mapFrom override (the base dispatch covers the composed sections).
//

#include "CvGameCoreDLL.h"        // PCH umbrella
#include "CvJsonEraInfo.h"

CvJsonEraInfo::CvJsonEraInfo() {}
